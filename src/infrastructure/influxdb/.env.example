# -----------------------------------------------------------------------------
# File: .env.example
# Summary: Sample environment overrides for docker-compose.yml to bootstrap
#          InfluxDB credentials and retention settings for PM500 telemetry.
# Usage:   Copy to .env in the same directory (or export values) before running
#          `docker compose up` so the container initializes with desired values.
# Depends: Consumed by Docker Compose; secrets should be replaced with secure
#          alternatives prior to production use.
# -----------------------------------------------------------------------------
INFLUXDB_USERNAME=admin
INFLUXDB_PASSWORD=changeme
INFLUXDB_ORG=pm500
INFLUXDB_BUCKET=telemetry
INFLUXDB_RETENTION=30d
INFLUXDB_TOKEN=pm500-token
