# -----------------------------------------------------------------------------
# File: bootstrap.env.example
# Summary: Example credentials passed to the InfluxDB container via
#          DOCKER_INFLUXDB_INIT_* environment variables for first-run setup.
# Usage:   Copy to src/infrastructure/influxdb/.env (or export variables) and
#          adjust values before executing `docker compose up`.
# Depends: Used by influxdb:2.x image during bootstrap; replace defaults with
#          secure secrets for non-development environments.
# -----------------------------------------------------------------------------
DOCKER_INFLUXDB_INIT_USERNAME=pm500
DOCKER_INFLUXDB_INIT_PASSWORD=changeme
DOCKER_INFLUXDB_INIT_ORG=pm500-lab
DOCKER_INFLUXDB_INIT_BUCKET=powermonitor
